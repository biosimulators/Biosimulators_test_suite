[tool.poetry]
name = "biosimulators-test-suite"
version = "0.1.89"
description = "Tools for validating that biosimulation tools are consistent with the BioSimulators standards"
authors = ["Center for Reproducible Modeling <info@biosimulators.org>"]
license = "MIT License (MIT)"
readme = "README.md"

[tool.poetry.dependencies]
python = "~3.10"

cement = "<=3.0.6"
docker = "<=5.0.3"
natsort = "<=8.1.0"
pypdf2 = "<=2.8.1"
requests = "<=2.28.1"
python-dateutil = "<2.9"
pyyaml = "<5.4"
simplejson = "<3.17.0"
termcolor = "<1.2"
biosimulators-utils = {git = "https://github.com/biosimulators/Biosimulators_utils.git", rev = "poetry-build", extras = ["bngl", "cellml", "lems", "neuroml", "sbml", "simularium", "escher", "containers", "logging", "rba", "smoldyn"]}

[tool.poetry.group.logging]
optional = true

[tool.poetry.group.logging.dependencies]

[tool.poetry.scripts]
biosimulators-test-suite = "biosimulators_test_suite.exec_cli:main"

[build-system]
requires = ["poetry-core", "flake8", "pytest"]
build-backend = "poetry.core.masonry.api"
